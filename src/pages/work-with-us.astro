---
import BaseLayout from '../layouts/BaseLayout.astro';
import BookingScripts from '../components/BookingScripts.astro';

const title = "Work with us · Pillexis Labs";
const description = "Two sprints. One operator. We sort the AI mess and the ops mess for Indian D2C teams who've outgrown their stack.";

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  name: 'Pillexis Labs · Sprint engagements',
  description,
  url: 'https://pillexislabs.com/work-with-us',
  provider: {
    '@type': 'Organization',
    name: 'Pillexis Labs',
    url: 'https://pillexislabs.com',
  },
  serviceType: 'AI & Operations consulting sprints',
  areaServed: { '@type': 'Country', name: 'India' },
  potentialAction: {
    '@type': 'ReserveAction',
    target: 'https://cal.com/pillexislabs/pillexis-labs-intro-call',
    result: { '@type': 'Reservation', name: '20-minute intro call' },
  },
};
---

<BaseLayout title={title} description={description} path="/work-with-us" jsonLd={jsonLd}>
  <style is:global>

    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg: #0a0a0a;
      --bg-elev: #111114;
      --surface: #141417;
      --surface-2: #18181c;
      --hairline: rgba(255, 255, 255, 0.08);
      --hairline-strong: rgba(255, 255, 255, 0.14);

      --red: #FF6363;
      --red-hot: #FF4D4D;
      --red-dim: rgba(255, 99, 99, 0.18);
      --red-faint: rgba(255, 99, 99, 0.08);
      --peach: #FFA48A;

      --text: #F5F5F7;
      --text-muted: #A1A1A6;
      --text-dim: #6E6E73;

      --sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      --mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, monospace;

      --r-sm: 6px;
      --r-md: 10px;
      --r-lg: 14px;
      --r-xl: 20px;

      --shadow-card: 0 1px 0 rgba(255, 255, 255, 0.04) inset, 0 8px 24px rgba(0, 0, 0, 0.4);
      --glow-red: 0 0 80px 0 rgba(255, 99, 99, 0.35);
      --glow-red-soft: 0 0 40px 0 rgba(255, 99, 99, 0.18);
    }

    html { scroll-behavior: smooth; background: var(--bg); }
    body {
      font-family: var(--sans);
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      letter-spacing: -0.011em;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }
    ::selection { background: var(--red-dim); color: #fff; }

    a { color: var(--text); text-decoration: none; }

    /* ── NAV ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      padding: 0.7rem 1.5rem;
      background: rgba(10, 10, 10, 0.6);
      backdrop-filter: blur(18px) saturate(180%);
      -webkit-backdrop-filter: blur(18px) saturate(180%);
      border-bottom: 1px solid var(--hairline);
    }
    .nav-inner {
      max-width: 1200px; margin: 0 auto;
      display: flex; align-items: center; justify-content: space-between;
    }
    .logo {
      display: inline-flex; align-items: center; gap: 0.55rem;
      font-size: 0.98rem; font-weight: 600; color: var(--text);
      letter-spacing: -0.01em;
    }
    .logo-dot {
      width: 10px; height: 10px; border-radius: 2px;
      background: var(--red);
      box-shadow: 0 0 12px rgba(255, 99, 99, 0.4);
    }
    .nav-links { display: flex; gap: 1.75rem; list-style: none; }
    .nav-links a {
      color: var(--text-muted); font-size: 0.85rem; font-weight: 500;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--text); }
    .nav-cta {
      background: var(--red);
      color: #fff !important;
      padding: 0.45rem 0.9rem;
      border-radius: 8px;
      font-size: 0.82rem; font-weight: 500;
      border: 1px solid rgba(255, 255, 255, 0.12);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.2), var(--glow-red-soft);
      transition: background 0.2s;
    }
    .nav-cta:hover { background: var(--red-hot); }

    /* ── BUTTONS ── */
    .btn-primary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.95rem 1.6rem;
      border-radius: 10px;
      background: var(--red);
      color: #fff;
      font-weight: 500; font-size: 0.98rem;
      border: 1px solid rgba(255, 255, 255, 0.12);
      box-shadow:
        inset 0 1px 0 rgba(255, 255, 255, 0.25),
        0 1px 2px rgba(0, 0, 0, 0.4),
        0 0 30px rgba(255, 99, 99, 0.35);
      transition: transform 0.15s, box-shadow 0.2s, background 0.2s;
    }
    .btn-primary:hover {
      background: var(--red-hot); color: #fff;
      transform: translateY(-1px);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.3), 0 0 50px rgba(255, 99, 99, 0.5);
    }
    .btn-secondary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.95rem 1.6rem;
      border-radius: 10px;
      background: var(--surface);
      color: var(--text);
      border: 1px solid var(--hairline);
      font-weight: 500; font-size: 0.98rem;
      transition: background 0.2s, border-color 0.2s;
    }
    .btn-secondary:hover { background: var(--surface-2); border-color: var(--hairline-strong); }

    /* ── SECTION ── */
    section { padding: 7rem 2rem; }
    .container { max-width: 1100px; margin: 0 auto; }
    .eyebrow {
      font-family: var(--mono);
      font-size: 0.72rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--text-dim);
      font-weight: 500;
      margin-bottom: 1.2rem;
    }
    .section-title {
      font-weight: 800;
      font-size: clamp(2.2rem, 4.5vw, 3.6rem);
      letter-spacing: -0.04em;
      line-height: 1.05;
      margin-bottom: 1.2rem;
    }
    .section-title em {
      font-style: normal;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .section-sub {
      font-size: 1.1rem;
      color: var(--text-muted);
      max-width: 640px;
      line-height: 1.6;
      margin-bottom: 2rem;
    }

    /* ── HERO ── */
    .hero {
      padding: 11rem 2rem 7rem;
      position: relative;
      overflow: hidden;
    }
    .hero::before {
      content: '';
      position: absolute;
      inset: 0;
      pointer-events: none;
      background:
        radial-gradient(circle at top right, rgba(255, 99, 99, 0.35), transparent 60%),
        radial-gradient(circle at 30% 80%, rgba(255, 140, 80, 0.12), transparent 55%);
    }
    .grid-bg {
      position: absolute; inset: 0; pointer-events: none; opacity: 0.4;
      background-image: radial-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px);
      background-size: 24px 24px;
      mask-image: linear-gradient(180deg, #000 0%, transparent 70%);
    }
    .hero-inner { position: relative; max-width: 1100px; margin: 0 auto; }

    .hero h1 {
      font-weight: 900;
      font-size: clamp(2.6rem, 6vw, 4.8rem);
      letter-spacing: -0.04em;
      line-height: 1.02;
      margin-bottom: 1.8rem;
      max-width: 760px;
    }
    .hero h1 em {
      font-style: italic;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      filter: drop-shadow(0 0 40px rgba(255, 99, 99, 0.45));
    }
    .hero-sub {
      font-size: 1.2rem;
      color: var(--text-muted);
      max-width: 540px;
      line-height: 1.55;
      margin-bottom: 2.5rem;
    }
    .hero-cta-row { display: flex; gap: 0.85rem; flex-wrap: wrap; }

    .hero-receipts {
      margin-top: 4.5rem;
      display: flex;
      gap: 1.5rem;
      flex-wrap: wrap;
      padding-top: 2rem;
      border-top: 1px solid var(--hairline);
    }
    .hero-receipt {
      flex: 1; min-width: 200px;
    }
    .hero-receipt .num {
      font-weight: 800;
      font-size: 2.4rem;
      letter-spacing: -0.03em;
      line-height: 1;
      background: linear-gradient(180deg, #fff 0%, var(--red) 130%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .hero-receipt .label {
      font-family: var(--mono);
      font-size: 0.72rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--text-dim);
      margin-top: 0.6rem;
    }

    /* ── RECEIPTS / CASE STUDY ── */
    .receipts { background: var(--bg); }
    .receipts-card {
      background: var(--surface);
      border: 1px solid var(--hairline);
      border-radius: var(--r-xl);
      padding: 3rem;
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow-card);
    }
    /* Subtle brand-mark divider between case studies */
    .case-divider {
      display: flex;
      align-items: center;
      gap: 0.9rem;
      max-width: 220px;
      margin: 3rem auto;
    }
    .case-divider-line {
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--hairline-strong), transparent);
    }
    .case-divider-mark {
      width: 8px;
      height: 8px;
      border-radius: 2px;
      background: var(--red);
      box-shadow: 0 0 10px rgba(255, 99, 99, 0.55);
      flex-shrink: 0;
    }
    .case-meta .case-link {
      color: var(--peach);
      text-decoration: underline;
      text-decoration-color: rgba(255, 164, 138, 0.4);
      text-underline-offset: 2px;
      transition: color 0.2s, text-decoration-color 0.2s;
    }
    .case-meta .case-link:hover {
      color: var(--red);
      text-decoration-color: var(--red);
    }
    .receipts-card::before {
      content: '';
      position: absolute;
      inset: 0;
      pointer-events: none;
      background: radial-gradient(circle at top right, rgba(255, 99, 99, 0.10), transparent 55%);
    }
    .receipts-card-inner { position: relative; z-index: 1; }
    .case-meta {
      display: flex; gap: 0.6rem; align-items: center;
      font-family: var(--mono);
      font-size: 0.7rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 1.5rem;
    }
    .case-meta .dot { width: 6px; height: 6px; border-radius: 50%; background: var(--red); box-shadow: 0 0 10px var(--red); }
    .case-title {
      font-weight: 800;
      font-size: clamp(1.8rem, 3.5vw, 2.8rem);
      letter-spacing: -0.035em;
      line-height: 1.1;
      margin-bottom: 1.5rem;
    }
    .case-title em {
      font-style: normal;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .case-body {
      color: var(--text-muted);
      font-size: 1.05rem;
      line-height: 1.7;
      max-width: 680px;
      margin-bottom: 1.5rem;
    }

    /* Bulleted breakdown of work shipped per case. Hairline-separated rows
       with a red arrow bullet. The "grouped" variant adds a mono-uppercase
       label above each bullet's body for multi-surface case studies. */
    .case-bullets {
      list-style: none;
      max-width: 720px;
      margin: 0 0 2.5rem;
      padding: 0;
    }
    .case-bullets li {
      position: relative;
      padding: 0.75rem 0 0.75rem 1.6rem;
      color: var(--text);
      font-size: 1rem;
      line-height: 1.55;
      border-bottom: 1px solid var(--hairline);
    }
    .case-bullets li:last-child { border-bottom: 0; }
    .case-bullets li::before {
      content: '→';
      position: absolute;
      left: 0;
      top: 0.75rem;
      color: var(--red);
      font-weight: 700;
    }
    .case-bullets.case-bullets-grouped li {
      padding: 1rem 0 1rem 1.6rem;
    }
    .case-bullets.case-bullets-grouped li::before { top: 1rem; }
    .case-bullets.case-bullets-grouped li strong {
      display: block;
      font-family: var(--mono);
      font-weight: 500;
      font-size: 0.72rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--peach);
      margin-bottom: 0.45rem;
    }

    .case-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      padding-top: 2rem;
      border-top: 1px solid var(--hairline);
    }
    .case-stat .num {
      font-weight: 800;
      font-size: 2.6rem;
      letter-spacing: -0.04em;
      line-height: 1;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 0.5rem;
    }
    .case-stat .label {
      font-size: 0.92rem;
      color: var(--text-muted);
      line-height: 1.4;
    }
    .case-work {
      margin-top: 2.5rem;
      display: flex;
      flex-wrap: wrap;
      gap: 0.6rem;
    }
    .work-chip {
      font-family: var(--mono);
      font-size: 0.72rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--text-muted);
      padding: 0.4rem 0.8rem;
      background: rgba(255, 255, 255, 0.04);
      border: 1px solid var(--hairline);
      border-radius: 100px;
    }

    /* ── OUR OWN PRODUCTS ── */
    .own-products { background: var(--bg); }
    .own-products-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem;
      margin-top: 3rem;
    }
    .own-product-card {
      background: var(--surface);
      border: 1px solid var(--hairline);
      border-radius: var(--r-lg);
      padding: 1.75rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      color: var(--text);
      text-decoration: none;
      transition: border-color 0.2s, transform 0.2s, box-shadow 0.2s;
    }
    .own-product-card:not(.own-product-card-static):hover {
      border-color: var(--red);
      transform: translateY(-2px);
      box-shadow: var(--glow-red-soft);
    }
    .own-product-card-static { cursor: default; }

    .own-product-meta {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 0.75rem;
      flex-wrap: wrap;
    }
    .own-product-status {
      display: inline-flex;
      align-items: center;
      gap: 0.45rem;
      font-family: var(--mono);
      font-size: 0.66rem;
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      padding: 0.35rem 0.7rem;
      border-radius: 100px;
      border: 1px solid var(--hairline);
      color: var(--text-muted);
      background: rgba(255, 255, 255, 0.03);
    }
    .own-product-status .dot {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--text-muted);
    }
    .own-product-status.live {
      color: var(--red);
      background: rgba(255, 99, 99, 0.1);
      border-color: rgba(255, 99, 99, 0.28);
    }
    .own-product-status.live .dot {
      background: var(--red);
      box-shadow: 0 0 8px var(--red);
    }
    .own-product-status.muted {
      color: var(--text-muted);
    }
    .own-product-status.experimental {
      color: #f59e0b;
      background: rgba(245, 158, 11, 0.10);
      border-color: rgba(245, 158, 11, 0.28);
    }
    .own-product-status.experimental .dot {
      background: #f59e0b;
      box-shadow: 0 0 8px #f59e0b;
    }
    .own-product-host {
      font-family: var(--mono);
      font-size: 0.7rem;
      letter-spacing: 0.06em;
      color: var(--text-dim);
    }
    .own-product-card:not(.own-product-card-static):hover .own-product-host {
      color: var(--red);
    }

    .own-product-name {
      font-family: var(--sans);
      font-weight: 700;
      font-size: 1.35rem;
      letter-spacing: -0.02em;
      color: var(--text);
    }
    .own-product-outcome {
      color: var(--text-muted);
      font-size: 0.98rem;
      line-height: 1.55;
    }
    .own-product-features {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      margin-top: auto;
      padding-top: 0.5rem;
    }
    .own-product-features span {
      font-family: var(--mono);
      font-size: 0.66rem;
      letter-spacing: 0.06em;
      color: var(--text-muted);
      padding: 0.3rem 0.6rem;
      background: rgba(255, 255, 255, 0.04);
      border: 1px solid var(--hairline);
      border-radius: 100px;
    }

    /* ── SPRINTS ── */
    .sprints {
      background: linear-gradient(180deg, var(--bg) 0%, #050505 100%);
    }
    .sprints-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.5rem;
      margin-top: 3rem;
    }
    .sprint-card {
      background: var(--surface);
      border: 1px solid var(--hairline);
      border-radius: var(--r-xl);
      padding: 2.5rem;
      position: relative;
      display: flex;
      flex-direction: column;
      transition: border-color 0.2s, transform 0.15s;
    }
    .sprint-card:hover {
      border-color: var(--hairline-strong);
      transform: translateY(-2px);
    }
    .sprint-card.featured {
      border: 1px solid rgba(255, 99, 99, 0.3);
      box-shadow: 0 0 60px rgba(255, 99, 99, 0.15);
    }
    .sprint-tag {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      font-family: var(--mono);
      font-size: 0.66rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--red);
      font-weight: 600;
      padding: 0.4rem 0.75rem;
      background: rgba(255, 99, 99, 0.08);
      border: 1px solid rgba(255, 99, 99, 0.2);
      border-radius: 100px;
      margin-bottom: 1.5rem;
      width: fit-content;
    }
    .sprint-name {
      font-weight: 800;
      font-size: 1.7rem;
      letter-spacing: -0.025em;
      margin-bottom: 0.4rem;
    }
    .sprint-name em {
      font-style: normal;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .sprint-pitch {
      color: var(--text-muted);
      font-size: 0.98rem;
      line-height: 1.55;
      margin-bottom: 1.5rem;
    }
    .sprint-price-row {
      display: flex;
      align-items: baseline;
      gap: 0.7rem;
      padding: 1rem 0;
      border-top: 1px solid var(--hairline);
      border-bottom: 1px solid var(--hairline);
      margin-bottom: 1.5rem;
    }
    .sprint-price {
      font-weight: 800;
      font-size: 1.8rem;
      letter-spacing: -0.025em;
      color: var(--text);
    }
    .sprint-duration {
      font-family: var(--mono);
      font-size: 0.78rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-dim);
    }
    .sprint-includes {
      list-style: none;
      margin-bottom: 1.5rem;
    }
    .sprint-includes li {
      font-size: 0.95rem;
      color: var(--text-muted);
      padding: 0.45rem 0;
      padding-left: 1.6rem;
      position: relative;
      line-height: 1.5;
    }
    .sprint-includes li::before {
      content: '→';
      position: absolute;
      left: 0;
      color: var(--red);
      font-weight: 700;
    }
    .sprint-card .btn-primary { margin-top: auto; width: fit-content; }

    /* ── HOW IT WORKS ── */
    .how { background: var(--bg); }
    .how-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-top: 3rem;
    }
    .how-step {
      background: var(--surface);
      border: 1px solid var(--hairline);
      border-radius: var(--r-lg);
      padding: 2rem;
      position: relative;
    }
    .how-step .num {
      font-family: var(--mono);
      font-size: 0.72rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 1rem;
      display: block;
    }
    .how-step h3 {
      font-weight: 700;
      font-size: 1.25rem;
      letter-spacing: -0.02em;
      margin-bottom: 0.6rem;
    }
    .how-step p {
      color: var(--text-muted);
      font-size: 0.95rem;
      line-height: 1.55;
    }

    /* ── FAQ ── */
    .faq { background: var(--bg); }
    .faq-list { margin-top: 2.5rem; }
    .faq-item {
      border-top: 1px solid var(--hairline);
      padding: 1.6rem 0;
    }
    .faq-item:last-child { border-bottom: 1px solid var(--hairline); }
    .faq-question {
      width: 100%;
      background: none;
      border: none;
      color: var(--text);
      font-family: var(--sans);
      font-weight: 600;
      font-size: 1.1rem;
      letter-spacing: -0.015em;
      text-align: left;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
    }
    .faq-question::after {
      content: '+';
      font-weight: 400;
      font-size: 1.5rem;
      color: var(--red);
      transition: transform 0.2s;
    }
    .faq-item.active .faq-question::after { content: '−'; }
    .faq-answer {
      max-height: 0;
      overflow: hidden;
      color: var(--text-muted);
      font-size: 1rem;
      line-height: 1.65;
      transition: max-height 0.3s, padding-top 0.3s;
    }
    .faq-item.active .faq-answer {
      max-height: 400px;
      padding-top: 1rem;
    }

    /* ── FINAL CTA ── */
    .cta-final {
      padding: 9rem 2rem;
      background: var(--bg);
      position: relative;
      overflow: hidden;
    }
    .cta-final::before {
      content: '';
      position: absolute;
      inset: 0;
      pointer-events: none;
      background:
        radial-gradient(ellipse 50% 50% at 50% 50%, rgba(255, 99, 99, 0.26), transparent 65%),
        radial-gradient(ellipse 70% 40% at 50% 50%, rgba(255, 140, 80, 0.10), transparent 75%);
    }
    .cta-final-inner {
      position: relative; z-index: 1;
      max-width: 800px; margin: 0 auto;
      text-align: center;
    }
    .cta-final h2 {
      font-weight: 800;
      font-size: clamp(2.5rem, 6vw, 4.2rem);
      line-height: 1.02;
      letter-spacing: -0.04em;
      margin-bottom: 1rem;
    }
    .cta-final h2 em {
      font-style: normal;
      background: linear-gradient(180deg, var(--peach) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .cta-final p {
      color: var(--text-muted);
      font-size: 1.1rem;
      margin: 1.4rem auto 2.4rem;
      max-width: 520px;
      line-height: 1.6;
    }
    .cta-bullets {
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin-top: 2.5rem;
      flex-wrap: wrap;
    }
    .cta-bullets div {
      font-family: var(--mono);
      font-size: 0.78rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-muted);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .cta-bullets div::before {
      content: '';
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--red);
      box-shadow: 0 0 10px var(--red);
    }

    /* ── FOOTER ── */
    footer {
      background: var(--bg);
      padding: 3rem 2rem 2rem;
      border-top: 1px solid var(--hairline);
    }
    .footer-inner {
      max-width: 1100px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 2rem;
    }
    .footer-logo {
      display: inline-flex; align-items: center; gap: 0.55rem;
      font-weight: 600; font-size: 1rem;
      margin-bottom: 0.5rem; letter-spacing: -0.01em;
    }
    .footer-left p { color: var(--text-dim); font-size: 0.8rem; }
    .footer-links { display: flex; gap: 3rem; }
    .footer-col h4 {
      font-family: var(--mono);
      font-size: 0.66rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--text-dim);
      margin-bottom: 0.85rem;
    }
    .footer-col a {
      display: block;
      color: var(--text-muted);
      font-size: 0.88rem;
      margin-bottom: 0.45rem;
      transition: color 0.2s;
    }
    .footer-col a:hover { color: var(--text); }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      .nav-links { display: none; }
      section { padding: 5rem 1.5rem; }
      .hero { padding: 8rem 1.5rem 5rem; }
      .sprints-grid { grid-template-columns: 1fr; }
      .how-grid { grid-template-columns: 1fr; }
      .case-stats { grid-template-columns: 1fr; gap: 1.5rem; }
      .own-products-grid { grid-template-columns: 1fr; }
      .footer-inner { flex-direction: column; }
      .footer-links { gap: 1.5rem; }
      .receipts-card { padding: 2rem 1.5rem; }
    }
  </style>


  <!-- NAV -->
  <nav>
    <div class="nav-inner">
      <a href="/" class="logo">
        <span class="logo-dot"></span>
        Pillexis Labs
      </a>
      <ul class="nav-links">
        <li><a href="/#about">About</a></li>
        <li><a href="/#products">Products</a></li>
        <li><a href="/work-with-us">Work with us</a></li>
      </ul>
      <a
        href="https://cal.com/pillexislabs/pillexis-labs-intro-call"
        target="_blank"
        rel="noopener"
        class="nav-cta"
        data-cal-link="pillexislabs/pillexis-labs-intro-call"
        data-cal-namespace="pillexis-labs-intro-call"
        data-cal-config='{"layout":"month_view"}'
        data-book-call="nav"
      >Book a 20-min call</a>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="grid-bg"></div>
    <div class="hero-inner">
      <div class="eyebrow">Ops + AI Systems · Built In-House</div>
      <h1>We build the stack<br/>your team <em>actually owns.</em></h1>
      <p class="hero-sub">
        We audit the AI stack, automate the ops workflows, and replace bloated SaaS with internal tools your team owns. Two fixed-price sprints. Code in your GitHub from day one.
      </p>
      <div class="hero-cta-row">
        <a
          href="https://cal.com/pillexislabs/pillexis-labs-intro-call"
          target="_blank"
          rel="noopener"
          class="btn-primary"
          data-cal-link="pillexislabs/pillexis-labs-intro-call"
          data-cal-namespace="pillexis-labs-intro-call"
          data-cal-config='{"layout":"month_view"}'
          data-book-call="hero"
        >Book a 20-min call →</a>
        <a href="#how" class="btn-secondary">See how we work</a>
      </div>

      <div class="hero-receipts">
        <div class="hero-receipt">
          <div class="num">31 → 9 hrs</div>
          <div class="label">Daily ops cut, same team</div>
        </div>
        <div class="hero-receipt">
          <div class="num">500K+</div>
          <div class="label">Users served · platform we built for</div>
        </div>
        <div class="hero-receipt">
          <div class="num">30 days</div>
          <div class="label">Fixed timeline, fixed price</div>
        </div>
      </div>
    </div>
  </section>

  <!-- RECEIPTS / CASE STUDY -->
  <section class="receipts">
    <div class="container">
      <div class="eyebrow">Receipts · the work behind the sprints</div>
      <h2 class="section-title">What we ship<br/><em>actually moves.</em></h2>
      <p class="section-sub">
        Two consumer platforms. Full-stack work: mobile apps, backends, admin panels, internal CRMs. Built end-to-end by a small team, shipped to real users.
      </p>

      <div class="receipts-card">
        <div class="receipts-card-inner">
          <div class="case-meta">
            <span class="dot"></span>
            India's largest mental wellness platform · 500K+ users
          </div>
          <h3 class="case-title">Daily team ops dropped from <em>31 hours to 9.</em><br/>With fewer team members. While revenue grew.</h3>
          <p class="case-body">
            Six months of full-stack work. We replaced a tangle of SaaS with internal tools the team owns.
          </p>
          <ul class="case-bullets">
            <li>Rebuilt the CRM from scratch</li>
            <li>Automated prescriptions, invoices, and session notifications</li>
            <li>AI-drafted clinical notes</li>
            <li>Mobile dashboards for the practitioner team</li>
            <li>Retired Typeform, a third-party OTP service, and a half-dozen Zapier flows</li>
            <li>Shipped two consumer mobile apps in parallel: Rocket Journal and Rocket Fuel</li>
            <li>Plus a vernacular Tier 2-3 product for Bharat</li>
          </ul>

          <div class="case-stats">
            <div class="case-stat">
              <div class="num">22 hrs</div>
              <div class="label">Daily team ops removed</div>
            </div>
            <div class="case-stat">
              <div class="num">6</div>
              <div class="label">SaaS subscriptions retired</div>
            </div>
            <div class="case-stat">
              <div class="num">2</div>
              <div class="label">Consumer apps shipped in parallel</div>
            </div>
          </div>

          <div class="case-work">
            <span class="work-chip">CRM rebuild</span>
            <span class="work-chip">AI clinical notes</span>
            <span class="work-chip">Mobile dashboards</span>
            <span class="work-chip">Automated comms</span>
            <span class="work-chip">Internal OTP</span>
            <span class="work-chip">Vernacular app</span>
            <span class="work-chip">2× consumer apps</span>
          </div>
        </div>
      </div>

      <div class="case-divider" aria-hidden="true">
        <span class="case-divider-line"></span>
        <span class="case-divider-mark"></span>
        <span class="case-divider-line"></span>
      </div>

      <div class="receipts-card">
        <div class="receipts-card-inner">
          <div class="case-meta">
            <span class="dot"></span>
            <a href="https://www.ojiapp.in/" target="_blank" rel="noopener" class="case-link">Oji</a> · Consumer AI agent platform for Bharat
          </div>
          <h3 class="case-title">AI for Bharat,<br/>on their phone, <em>in their language.</em></h3>
          <p class="case-body">
            Built so a non-English speaker in a Tier 2 city could open an AI assistant and actually use it. Three pieces of work, one outcome.
          </p>
          <ul class="case-bullets case-bullets-grouped">
            <li>
              <strong>Built for Bharat from day one</strong>
              Eight languages live at launch (English plus seven Indic). Not a translated American product. The Hindi-first user gets the same experience as the English-first user, in the language they actually think in.
            </li>
            <li>
              <strong>Multi-persona, one tap away</strong>
              AI agents for the things people actually do every day: government services, earning, English learning, astrology, health. The user picks the persona for the moment they're in, instead of being stuck in one general chat.
            </li>
            <li>
              <strong>A team that ships new agents without an engineer</strong>
              The admin panel hands content control back to Oji's team. New categories, fresh home sections, new agents, all manageable in-house. Engineering stops being the bottleneck for new content.
            </li>
          </ul>

          <div class="case-stats">
            <div class="case-stat">
              <div class="num">8</div>
              <div class="label">Languages live at launch (English + 7 Indic)</div>
            </div>
            <div class="case-stat">
              <div class="num">5+</div>
              <div class="label">AI personas live (gov, earning, learning, more)</div>
            </div>
            <div class="case-stat">
              <div class="num">0</div>
              <div class="label">Engineering hours for routine content updates</div>
            </div>
          </div>

          <div class="case-work">
            <span class="work-chip">Android app</span>
            <span class="work-chip">Admin / CRM</span>
            <span class="work-chip">Multi-language launch</span>
            <span class="work-chip">Multi-persona agents</span>
            <span class="work-chip">Real-time chat</span>
            <span class="work-chip">Bharat-first design</span>
            <span class="work-chip">In-house content shipping</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- OUR OWN PRODUCTS -->
  <section class="own-products" id="our-products">
    <div class="container">
      <div class="eyebrow">Our own products · receipts of receipts</div>
      <h2 class="section-title">We don't just consult on shipping.<br/><em>We ship.</em></h2>
      <p class="section-sub">
        Four products across four domains, built by the same team that does the client work. When we tell you what works in a sprint, it's not from a book. It's from shipping our own.
      </p>

      <div class="own-products-grid">
        <a href="https://productlogz.com" target="_blank" rel="noopener" class="own-product-card">
          <div class="own-product-meta">
            <span class="own-product-status live"><span class="dot"></span>Live</span>
            <span class="own-product-host">productlogz.com</span>
          </div>
          <h3 class="own-product-name">Productlogz</h3>
          <p class="own-product-outcome">
            Helps SaaS teams stop arguing about what to build, by letting their customers vote on what matters and watching that turn into a public roadmap.
          </p>
          <div class="own-product-features">
            <span>Feedback boards</span>
            <span>User voting</span>
            <span>Public roadmaps</span>
            <span>Changelogs</span>
          </div>
        </a>

        <a href="https://quotesmatic.com" target="_blank" rel="noopener" class="own-product-card">
          <div class="own-product-meta">
            <span class="own-product-status live"><span class="dot"></span>Live · iOS</span>
            <span class="own-product-host">quotesmatic.com</span>
          </div>
          <h3 class="own-product-name">Quotesmatic</h3>
          <p class="own-product-outcome">
            Helps people become who they want to be, one sentence at a time. Pick a future self, write a line they would say, live with it for a week.
          </p>
          <div class="own-product-features">
            <span>Future self framework</span>
            <span>Daily mindset practice</span>
            <span>iOS</span>
            <span>Free + Premium</span>
          </div>
        </a>

        <a href="https://migrainelogs.com" target="_blank" rel="noopener" class="own-product-card">
          <div class="own-product-meta">
            <span class="own-product-status muted"><span class="dot"></span>Coming soon</span>
            <span class="own-product-host">migrainelogs.com</span>
          </div>
          <h3 class="own-product-name">Migraine Logs</h3>
          <p class="own-product-outcome">
            Helps migraine sufferers find their real triggers, even when they're in the middle of an attack and can't fill out a long form. Logs in seconds with their voice.
          </p>
          <div class="own-product-features">
            <span>Voice logging</span>
            <span>Weather + sleep auto-attached</span>
            <span>Doctor-ready summary</span>
            <span>iOS + Android</span>
          </div>
        </a>

        <div class="own-product-card own-product-card-static">
          <div class="own-product-meta">
            <span class="own-product-status experimental"><span class="dot"></span>Very experimental</span>
            <span class="own-product-host">macOS only</span>
          </div>
          <h3 class="own-product-name">Forge</h3>
          <p class="own-product-outcome">
            Helps engineers running many AI coding agents at once stop drowning in browser tabs and terminal windows. One desktop app, every agent in its own lane.
          </p>
          <div class="own-product-features">
            <span>Many agents, one window</span>
            <span>Each agent in its own lane</span>
            <span>Linear, GitHub, Slack baked in</span>
            <span>macOS desktop</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="how" id="how">
    <div class="container">
      <div class="eyebrow">How it works · zero ambiguity</div>
      <h2 class="section-title">Three steps from <em>mess to shipped.</em></h2>

      <div class="how-grid">
        <div class="how-step">
          <span class="num">Step 01</span>
          <h3>20-minute call</h3>
          <p>You describe the mess. We ask 4 questions. By the end of the call, you'll know whether a sprint fits and roughly what it'll cost.</p>
        </div>
        <div class="how-step">
          <span class="num">Step 02</span>
          <h3>One-page proposal</h3>
          <p>Within 48 hours, you get a scope doc: what we'll build, what we won't, the timeline, the price, and the exit criteria. No retainer fine print.</p>
        </div>
        <div class="how-step">
          <span class="num">Step 03</span>
          <h3>Sprint &amp; ship</h3>
          <p>We build in your GitHub from day 1. Weekly demos. Async Slack. End of sprint: you own everything. We can leave or stick around for handover.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq" id="faq">
    <div class="container" style="max-width: 800px;">
      <div class="eyebrow">FAQ · what founders ask first</div>
      <h2 class="section-title">The honest <em>answers.</em></h2>

      <div class="faq-list">
        <div class="faq-item">
          <button class="faq-question">What if the work doesn't move the metric?</button>
          <div class="faq-answer">
            We scope the metric upfront: daily ops hours, subscription spend, prompt eval pass rate, whatever's bleeding. If we miss the target by day 30, we refund the final milestone (typically 30% of the sprint). We've never had to.
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question">Do we get the code?</button>
          <div class="faq-answer">
            Yes. From day 1. We push to your GitHub org, not ours. You own every line, every prompt, every credential. We don't keep copies.
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question">Who actually does the work?</button>
          <div class="faq-answer">
            Anurag (founder) is on every project. Depending on scope, 1–2 engineers and a designer join. No offshore handoff, no junior overload, no agency-style staff swaps mid-sprint.
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question">Why two sprints instead of a retainer?</button>
          <div class="faq-answer">
            Retainers reward keeping you on retainer. Fixed-scope sprints reward shipping. After delivery, you can hire us for another sprint or vanish. We'll never DM you about renewal.
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question">We're in healthtech / mental wellness. Can you help?</button>
          <div class="faq-answer">
            Sorry, no. We have an existing exclusive engagement in that space. Most other Indian D2C verticals are open.
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question">Do you take international clients?</button>
          <div class="faq-answer">
            Right now we're focused on Indian D2C brands ₹50L–₹10Cr ARR. If you're outside that band, write to <a href="mailto:contact@pillexislabs.com" style="color:var(--red);">contact@pillexislabs.com</a> and we'll tell you honestly whether we're a fit.
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="cta-final" id="book">
    <div class="cta-final-inner">
      <div class="eyebrow" style="justify-content:center;text-align:center;">Book a 20-min call</div>
      <h2>Tell us where the <em>mess is.</em></h2>
      <p>
        Pick a slot. Twenty minutes. By the end of the call, you'll know whether a sprint fits, what it'll cost, and what changes inside thirty days.
      </p>
      <a
        href="https://cal.com/pillexislabs/pillexis-labs-intro-call"
        target="_blank"
        rel="noopener"
        class="btn-primary"
        style="font-size:1.05rem;padding:1.1rem 1.9rem;"
        data-cal-link="pillexislabs/pillexis-labs-intro-call"
        data-cal-namespace="pillexis-labs-intro-call"
        data-cal-config='{"layout":"month_view"}'
        data-book-call="final"
      >
        Open the calendar →
      </a>
      <div class="cta-bullets">
        <div>No sales call</div>
        <div>No retainer pitch</div>
        <div>You keep the scope doc either way</div>
      </div>
      <p style="margin-top:3rem;font-size:0.9rem;color:var(--text-dim);">
        Prefer email? <a href="mailto:contact@pillexislabs.com" style="color:var(--red);">contact@pillexislabs.com</a>
      </p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-inner">
      <div class="footer-left">
        <div class="footer-logo">
          <span class="logo-dot"></span>
          Pillexis Labs
        </div>
        <p>&copy; 2026 Pillexis Labs Pvt Ltd</p>
      </div>
      <div class="footer-links">
        <div class="footer-col">
          <h4>Work</h4>
          <a href="#how">How we work</a>
          <a href="#faq">FAQ</a>
          <a
            href="https://cal.com/pillexislabs/pillexis-labs-intro-call"
            target="_blank"
            rel="noopener"
            data-cal-link="pillexislabs/pillexis-labs-intro-call"
            data-cal-namespace="pillexis-labs-intro-call"
            data-cal-config='{"layout":"month_view"}'
            data-book-call="footer"
          >Book a call</a>
        </div>
        <div class="footer-col">
          <h4>Resources</h4>
          <a href="/blog">Blog</a>
          <a href="/tools">Free tools</a>
          <a href="/rss.xml">RSS</a>
        </div>
        <div class="footer-col">
          <h4>Company</h4>
          <a href="/#about">About</a>
          <a href="/#products">Products</a>
          <a href="/#philosophy">Philosophy</a>
          <a href="mailto:contact@pillexislabs.com">Contact</a>
        </div>
      </div>
    </div>
  </footer>

  <script>
    document.querySelectorAll('.faq-question').forEach(btn => {
      btn.addEventListener('click', () => {
        const item = btn.parentElement;
        const wasActive = item.classList.contains('active');
        document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('active'));
        if (!wasActive) item.classList.add('active');
      });
    });
  </script>

  <BookingScripts />

</BaseLayout>
